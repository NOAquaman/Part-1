#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
	int a,b,time,day = 0;
	for(int i=0;i<=6;i++)
	{
		scanf("%d %d",&a,&b);
		if(a+b>8&&a+b>time)
		{
			time = a + b;
			day = i+1;
		}
	}
	printf("%d",day);
	return 0;
}
