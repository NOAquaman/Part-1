#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
	long n = 0;
	int x = 0,ans = 0;
	scanf("%ld %d",&n,&x);
	for(int i = 1;i <= n;i++)
	{
		for(int j = 10;j/10<=i;j=j*10)
		{
			if((i%j)/(j/10)==x)	ans++;
		}
	}
	printf("%d",ans);
}
